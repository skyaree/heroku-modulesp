name: Keep Alive Pinger

on:
  schedule:
     - cron: '*/15 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render API
        run: curl -s 'https://heroku-modulesp.onrender.com/api/v1/modules'
